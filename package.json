{
  "name": "jelly-world",
  "version": "0.0.1",
  "private": true,
  "description": "A cute 3D browser exploration platformer — play a soft jelly, absorb smaller jellies, climb to the top.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.163.0",
    "three-mesh-bvh": "^0.7.0",
    "zzfx": "^1.1.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/three": "^0.163.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
